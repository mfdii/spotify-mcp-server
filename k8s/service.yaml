---
apiVersion: v1
kind: Service
metadata:
  name: spotify-mcp-server
  namespace: n8n
  labels:
    app: spotify-mcp-server
spec:
  selector:
    app: spotify-mcp-server
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP
